

<div class="container-fluid wt-page-content">
    <h2><i class="fa fa-eye-slash"></i> Ignorierte Fehler</h2>

    <p>Hier sind alle Fehler aufgelistet, die dauerhaft ausgeblendet wurden.</p>

    <?php if (empty($ignoredErrors)): ?>
        <div class="alert alert-info">
            Keine ignorierten Fehler gefunden.
        </div>
    <?php else: ?>
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>Datum</th>
                    <th>Person</th>
                    <th>Fehler-Code</th>
                    <th>Ignoriert von</th>
                    <th>Aktion</th>
                </tr>
            </thead>
            <tbody>
                <?php foreach ($ignoredErrors as $error): ?>
                    <tr>
                        <td><?= e($error->created_at) ?></td>
                        <td>
                            <a href="<?= e(route(Fisharebest\Webtrees\Http\RequestHandlers\IndividualPage::class, ['tree' => $tree->name(), 'xref' => $error->xref])) ?>">
                                <?= $error->person_name ?>
                            </a>
                        </td>
                        <td>
                            <code><?= e($error->error_code) ?></code><br>
                            <small class="text-muted"><?= e(\Wolfrum\Datencheck\Services\ValidationConstants::getLabel($error->error_code, $lang)) ?></small>
                        </td>
                        <td><?= e($error->user_name) ?></td>
                        <td>
                            <form method="post" action="<?= e(route('module', ['module' => $module_name, 'action' => 'AdminIgnored', 'tree' => $tree->name()])) ?>" onsubmit="return confirm('Soll dieser Fehler wirklich wieder angezeigt werden?');">
                                <?= csrf_field() ?>
                                <input type="hidden" name="xref" value="<?= e($error->xref) ?>">
                                <input type="hidden" name="code" value="<?= e($error->error_code) ?>">
                                <button type="submit" class="btn btn-sm btn-danger">
                                    <i class="fa fa-trash"></i> Löschen
                                </button>
                            </form>
                        </td>
                    </tr>
                <?php endforeach; ?>
            </tbody>
        </table>
    <?php endif; ?>
    
    <div style="margin-top: 20px;">
        <a href="<?= e(route(Fisharebest\Webtrees\Http\RequestHandlers\TreePage::class, ['tree' => $tree->name()])) ?>" class="btn btn-secondary">
            <i class="fa fa-arrow-left"></i> Zurück zum Stammbaum
        </a>
    </div>
</div>
